# M1-10 适配器监督器 —— 任务证据

| 项 | 内容 |
|---|---|
| 任务 | M1-10 适配器监督器（实施计划 v1.11 §2） |
| 对应决策 | D5、评审 #3/#4/#5；ADR-003/ADR-004 |
| 依赖 | M1-09（线协议骨架）、M1-03（存储） |
| 交付日期 | 2026-09-17 |
| 验证入口 | `pnpm verify:m1-10`（`scripts/test/m1-10/verify-m1-10.mjs`） |
| 证据运行环境 | Windows 11 x64，rustc 1.98.1，Node v22（本机）；Linux/macOS 断言见「未验证项」 |

---

## 0. 交付物与变更文件清单

```
crates/aether-adapters/Cargo.toml                    （改：process-wrap / sysinfo / windows 依赖 + 夹具 bin）
crates/aether-adapters/src/lib.rs                    （改：导出 supervisor 模块与 ProcessTerminationTarget）
crates/aether-adapters/src/protocol.rs               （改：status_reason 词典 +storage_backpressure/heartbeat_failed/crashed + ALL）
crates/aether-adapters/src/process.rs                （改：spawn 进程组/Job Object；终止序列接入；taskkill 硬超时约束）
crates/aether-adapters/src/supervisor/mod.rs         （新：模块与再导出）
crates/aether-adapters/src/supervisor/state.rs       （新：D5 状态机 + 观察者 + status_changed 载荷）
crates/aether-adapters/src/supervisor/backoff.rs     （新：1/2/4/8/16/30s 退避 + 60s ≥5 次熔断）
crates/aether-adapters/src/supervisor/heartbeat.rs   （新：10s/5s/连续 3 次失败）
crates/aether-adapters/src/supervisor/admission.rs   （新：官方白名单准入）
crates/aether-adapters/src/supervisor/ledger.rs      （新：PID 台账三条件与启动清理）
crates/aether-adapters/src/supervisor/resources.rs   （新：5s 采样 + RSS/CPU 持续超限告警）
crates/aether-adapters/src/supervisor/termination.rs （新：D5 终止序列逐步硬超时）
crates/aether-adapters/src/supervisor/runtime.rs     （新：监督器与注册表、retry/enable、预热）
crates/aether-adapters/src/supervisor/clock.rs       （新：可注入时钟）
crates/aether-adapters/src/bin/aether-adapter-fixture.rs （新：故障注入夹具；T5a/T5b/M2-08/M4-01 可复用）
crates/aether-adapters/tests/common/mod.rs           （改：夹具/记录观察者/pid_alive 等测试支持）
crates/aether-adapters/tests/m1_10_ledger.rs         （新：台账三条件集成）
crates/aether-adapters/tests/m1_10_termination.rs    （新：终止序列/平台断言集成）
crates/aether-adapters/tests/m1_10_supervisor.rs     （新：监督器全流程集成）
scripts/test/m1-10/verify-m1-10.mjs                  （新：验证脚本）
package.json / scripts/test/ci-local.mjs             （改：注册 verify:m1-10）
Cargo.lock                                           （由 cargo 更新：process-wrap/sysinfo 等）
```

`git diff --stat`（仅本任务范围）：

```
 crates/aether-adapters/Cargo.toml          |  21 ++
 crates/aether-adapters/src/lib.rs          |   6 +-
 crates/aether-adapters/src/process.rs      | 325 ++++++++++++++++++++++++++---
 crates/aether-adapters/src/protocol.rs     |  49 ++++-
 crates/aether-adapters/tests/common/mod.rs | 168 +++++++++++++++
 package.json                               |   3 +
 scripts/test/ci-local.mjs                  |   8 +
 7 files changed, 538 insertions(+), 42 deletions(-)
```

> 注：`crates/aether-adapters/src/supervisor/`、`src/bin/`、`tests/m1_10_*.rs`、`scripts/test/m1-10/` 为新文件（上方 `git diff --stat` 不含未跟踪文件）。

---

## 1. DoD 逐条证据

### DoD① 状态机与 `runtimes.status/status_reason` 一一对应；每次转移广播 `runtime.status_changed`

- 验证方式：单测（状态枚举与 DDL 逐值对齐、合法转移表、非法转移拒绝、每次转移产出一条
  `StatusChange` 且 `event_type == runtime.status_changed`、payload 与附录 B 形状一致）；
  集成（`warmup_ready_transitions_and_ledger_record` 断言转移序列与事件类型）。
- 命令与输出：

```
$ cargo test -p aether-adapters --lib supervisor::state
test supervisor::state::tests::runtime_status_enum_matches_db_enum_in_order ... ok
test supervisor::state::tests::status_reason_dictionary_values_are_from_d5 ... ok
test supervisor::state::tests::legal_transition_table_covers_d5_and_recovery_paths ... ok
test supervisor::state::tests::happy_path_produces_one_change_per_transition ... ok
test supervisor::state::tests::illegal_transition_is_rejected_without_mutation ... ok
test supervisor::state::tests::disabled_requires_reason_and_ready_clears_reason ... ok
test supervisor::state::tests::recovery_paths_are_disabled_to_cold_to_starting ... ok
test supervisor::state::tests::payload_matches_appendix_b_shape ... ok
test result: ok. 8 passed; 0 failed
```

```
$ cargo test -p aether-adapters --test m1_10_supervisor warmup_ready
test warmup_ready_transitions_and_ledger_record ... ok
```
（断言转移序列 `cold→starting→ready`、每条 `event_type.as_str() == "runtime.status_changed"`、reason 为空。）

- 恢复转移（ADR-004/评审 #7）：`degraded → starting → ready`（心跳/崩溃重启集成断言）、
  `disabled → cold → starting`（retry/enable 集成断言，见 DoD⑦）。

### DoD② 退避 1/2/4/8/16/30s 与熔断（60s ≥5 次崩溃 → `disabled + crash_loop`）

- 验证方式：纯逻辑单测（手动时钟精确推进）+ 集成熔断恢复。
- 命令与输出：

```
$ cargo test -p aether-adapters --lib supervisor::backoff
test supervisor::backoff::tests::backoff_curve_is_1_2_4_8_16_30_then_capped ... ok
test supervisor::backoff::tests::five_crashes_within_60s_break_the_circuit ... ok
test supervisor::backoff::tests::crash_window_rolls_off_after_60s ... ok
test supervisor::backoff::tests::mark_stable_resets_backoff_level_but_keeps_crash_window ... ok
test supervisor::backoff::tests::manual_reset_clears_everything ... ok
test supervisor::backoff::tests::constants_match_d5 ... ok
test result: ok. 6 passed; 0 failed
```

```
$ cargo test -p aether-adapters --test m1_10_supervisor crash_loop
test crash_loop_circuit_breaker_then_enable_recovers ... ok
```
（连续 5 次崩溃注入：第 5 次返回 `CircuitBroken { crashes_in_window: 5 }`，
`runtimes.status=disabled`、`status_reason=crash_loop`、审计 `circuit_broken`。）

### DoD③ 进程组与 Windows Job Object；终止序列逐步硬超时，每步平台断言

- 验证方式：单测（硬超时：脚本化「永不退出」目标，逐步骤 `timeout_ms` 与预算一致，
  graceful 失败落到 force、RPC 失败继续）；集成（真实进程树 + 平台机制断言）。
- 真实输出（Windows 本机，`--nocapture`）：

```
$ cargo test -p aether-adapters --test m1_10_termination -- --nocapture
[m1-10] Windows 终止机制序列：["shutdown_rpc", "taskkill_tree", "terminate_job_object"]
test termination_sequence_platform_mechanisms_reap_whole_tree ... ok
test force_kill_reaps_tree_via_job_object_or_process_group ... ok
test graceful_step_is_bounded_and_platform_correct ... ok
test non_linux_platform_assertion_is_explicitly_skipped ... ok
test result: ok. 4 passed; 0 failed
```

说明（平台行为，实测）：
- Windows 控制台进程上 `taskkill /PID x /T`（无 `/F`）返回 EC 128
  （"can only be terminated forcefully"），**属预期**；序列随即执行步骤 3
  `TerminateJobObject` 整树回收（机制名 `terminate_job_object`），父+子进程全部消失；
  步骤 4 `taskkill /PID x /T /F` 为兜底（代码路径 `taskkill_tree_force`）。
- 硬超时证据：`taskkill` 以 `tokio` 子进程运行，超出步骤预算即被中止
  （`process.rs::taskkill_bounded`，预算 = 步骤硬超时 − 250ms 余量）；
  单测 `full_sequence_steps_run_in_d5_order_when_target_never_exits` 断言 RPC 5s /
  graceful 5s / force 3s / fallback 2s 的逐步硬超时，总预算 ≤15s（D5 重启预算）。
- Unix（`#[cfg(unix)]`，本机未运行，见「未验证项」）：`ProcessSession` 调 `nix::unistd::setsid`
  （process-wrap 8.2.1 源码），终止为 `SIGTERM`→`SIGKILL` 到 `-pgid`；Linux 断言
  `pgid == pid`（`/proc/<pid>/stat` 第 5 字段）。
- Windows `CREATE_NEW_PROCESS_GROUP`：spawn 经 `CreationFlags(0x00000200)` + `JobObject`
  （`process-wrap::tokio`）；常量断言 `windows_creation_flag_is_new_process_group`。
  Job Object 归属为行为断言（整树回收），非沙箱（ADR-004，代码注释明示）。

### DoD④ PID 台账三条件

- 验证方式：集成（真实进程夹具）：全命中→回收；启动时间不符→不杀；token 不符→不杀；
  PID 已不存在→仅移除陈旧记录；监督器启动清理审计。
- 命令与输出：

```
$ cargo test -p aether-adapters --test m1_10_ledger
test three_conditions_all_matched_reclaims_real_process ... ok
test alive_process_with_start_time_mismatch_is_never_killed ... ok
test alive_process_without_launch_token_in_cmdline_is_never_killed ... ok
test dead_pid_record_is_removed_without_kill ... ok
test supervisor_cleanup_orphans_reclaims_and_audits ... ok
test result: ok. 5 passed; 0 failed
```

台账文件布局：`~/.aether/run/adapters.json`（`ledger.rs::default_ledger_path`，
Windows `USERPROFILE` / Unix `HOME`，字段 `{adapter_id,pid,start_time_epoch,launch_token,cmdline_hash}`，
原子落盘；单测 `default_ledger_path_matches_d5_layout`）。spawn 注入 `--launch-token=<ULID>`。

### DoD⑤ 非官方 manifest → `disabled + untrusted` + 审计

```
$ cargo test -p aether-adapters --lib supervisor::admission
test supervisor::admission::tests::third_party_manifest_requires_trusted ... ok
test supervisor::admission::tests::self_claimed_official_id_must_still_be_allowlisted ... ok
test supervisor::admission::tests::official_builtin_is_admitted ... ok
test supervisor::admission::tests::custom_allowlist_is_respected ... ok
test supervisor::admission::tests::allowlist_contains_ddl_documented_ids ... ok
test supervisor::admission::tests::manifest_builder_defaults_are_safe ... ok
test result: ok. 6 passed; 0 failed

$ cargo test -p aether-adapters --test m1_10_supervisor untrusted
test untrusted_manifest_is_disabled_with_audit ... ok
```
（断言 `disabled + status_reason=untrusted` + `AuditKind::AdmissionRejected` + 不登记台账 +
`runtime_enable` 返回 `NeedsRemedy`。）

### DoD⑥ 启动即崩：stderr 尾 50 行 + `start_failed`

```
$ cargo test -p aether-adapters --test m1_10_supervisor start_failure
test start_failure_captures_stderr_tail_of_50_lines ... ok
```
（夹具输出 60 行 stderr 后退出：捕获恰为 50 行，首行 `#10 fixture stderr line`、
末行 `#59 fixture stderr line`；状态 `disabled + start_failed`。）

### DoD⑦ `runtime_retry` / `runtime_enable` 参数与状态校验；`disabled → cold → starting`

```
$ cargo test -p aether-adapters --lib supervisor::runtime
test supervisor::runtime::tests::retry_requires_disabled_start_failed ... ok
test supervisor::runtime::tests::retry_from_disabled_start_failed_transitions_cold_then_starting ... ok
test supervisor::runtime::tests::enable_blocks_untrusted_until_remedied ... ok
test supervisor::runtime::tests::enable_from_crash_loop_transitions_to_cold ... ok
test result: ok. ...

$ cargo test -p aether-adapters --test m1_10_supervisor runtime_retry
test runtime_retry_requires_whitelist_and_start_failed ... ok
```
（白名单外 runtime_id → `UnknownRuntime`；retry 仅 `disabled + start_failed`；
enable 仅 `disabled` 且 `untrusted/version_mismatch` → `NeedsRemedy`；
转移序列断言含 `disabled → cold → starting`。）

---

## 2. 验证脚本与全量结果

```
$ pnpm verify:m1-10
===== verify-m1-10 =====
PASS  期望=0 实际=0  cargo test -p aether-adapters --no-run（预构建测试目标与夹具 bin）
PASS  期望=0 实际=0  cargo test -p aether-adapters --lib（状态机/退避曲线/心跳/准入/台账三条件/终止序列硬超时）
PASS  期望=0 实际=0  编译 Mock 适配器单文件（bun build --compile，回归用）
PASS  期望=0 实际=0  回归 m1_09_consistency（真实 Mock：握手/流式/中断/dispose/shutdown）
PASS  期望=0 实际=0  回归 m1_09_robustness（真实 Mock：健壮性注入/版本不匹配/2MiB 契约）
PASS  期望=0 实际=0  m1_10_ledger（三条件全命中回收；时间/token 不符不杀；陈旧记录清理；监督器启动清理审计）
PASS  期望=0 实际=0  m1_10_termination（整树回收/逐步硬超时/平台机制断言，机制序列见输出）
PASS  期望=0 实际=0  m1_10_supervisor（预热 ready、心跳 ×3 → 重启、崩溃恢复、stderr 尾 50、untrusted、crash_loop、retry/enable）
----- 全部通过 -----
```

> 顺序说明：M1-09 回归先于 M1-10 夹具测试执行（夹具进程在 Windows 上短暂占用可执行文件，
> 避免与 cargo 重链接产生文件锁竞争）。

补充门禁（本机实际运行）：

```
$ cargo test -p aether-adapters --lib
test result: ok. 103 passed; 0 failed（其中 supervisor::* 58 项）
$ cargo test -p aether-adapters
m1_09_consistency 10 passed / m1_09_robustness 11 passed /
m1_10_ledger 5 passed / m1_10_supervisor 8 passed / m1_10_termination 4 passed

$ cargo clippy -p aether-adapters --all-targets -- -D warnings
Finished `dev` profile [unoptimized + debuginfo] target(s) in 5.31s   （0 告警）

$ cargo fmt --all -- --check
fmt exit=0

$ cargo deny check licenses bans sources
bans ok, licenses ok, sources ok
```

---

## 3. 常量级调参记录（AGENTS §3 MAY）

| 项 | 取值 | 说明 |
|---|---|---|
| `SupervisorConfig::backoff_override` | `None`（生产） | 生产严格执行 D5 曲线 1/2/4/8/16/30s；集成测试用 20ms 压缩，曲线本身由单测断言 |
| 集成测试心跳/握手/终止预算 | 200ms/150ms、3s | 常量级压缩以缩短 CI；D5 默认值（10s/5s/3 次、10s、5s/5s/3s/2s）由 `config_defaults_are_d5` 等单测锁定 |
| Windows `taskkill` 内侧超时 | 步骤硬超时 − 250ms | 保证「逐步硬超时」不被阻塞的下发动作吞掉；超时即中止 taskkill 并进入下一步 |
| `status_reason` 词典新增 | `heartbeat_failed`、`crashed`、`storage_backpressure` | `runtimes.status_reason` 为 TEXT 无 CHECK；D5 原文为「如」清单，ADR-003 明确要求 `storage_backpressure`；新增值只增不改语义 |
| `mark_stable`（退避复位） | Ready 持续 ≥60s 复位退避级数 | D5 未定义复位点；崩溃窗口仍按 60s 滚动（不削弱熔断判定） |
| 资源告警重置 | 回落即清零 | D5：超限需「持续 60s」；回落需重新累计 |

---

## 4. 未验证项与边界（明确标注）

1. **Unix 平台断言未在本机运行**：`linux_spawn_uses_setsid_process_group`（`/proc` pgrp 断言）
   为 `#[cfg(target_os = "linux")]`，本机 Windows 显示 `SKIP`。
   **（2026-09-18 更新：已由 CI 三平台矩阵关闭——ubuntu-latest/macos-latest 真实执行并通过，
   见 §5；macOS 断言为 `#[cfg(target_os = "macos")]` + `ps -o pgid=` 交叉验证。）**
2. **资源告警的真实超限路径未在集成触发**：无 1GB/200% 夹具；逻辑由
   `resources.rs` 单测覆盖（RSS/CPU 持续 60s、回落复位、边界相等不算超限），
   集成仅断言采样可读且不误报。
3. **`runtime_retry`/`runtime_enable` 的 IPC 接线未在本任务完成**：
   命令面 DTO/参数格式校验由 M1-08 交付（`ipc/dto.rs`、`ipc/commands.rs` 已注册并测试）；
   本任务交付命令语义（白名单 + 状态校验 + 转移断言）。将监督器注入 `IpcBackend`
   属 **M2-01**（依赖 M1-05、M1-10）范围；本任务不改动 `aether-tauri`（避免与并行任务冲突）。
4. **`CREATE_NEW_PROCESS_GROUP` 无运行期可见性断言**：Windows 未提供安全只读查询
   （`GetProcessTimes`/Job 查询不含创建标志；工作区 lint 禁止 unsafe FFI），
   以常量单测 + Job Object 行为断言（整树回收）组合覆盖。
5. **覆盖率门禁未在本机复跑**：`pnpm ci:local`（含 `cargo llvm-cov`）由批次出口统一执行；
   本任务新增模块自带单测/集成，未修改既有测试断言。
6. **Mock 适配器 CLI 尚未接受 `--launch-token`（交接项，不属本任务文件范围）**：
   D5 要求监督器 spawn 时注入 `--launch-token=<ULID>`；本任务以夹具 bin 全量验证该契约。
   `packages/adapter-mock` 的 `parseArgs` 目前对未知参数报错退出，M2-01/M2-02M 接入监督器前
   需在该 CLI（或 adapter-sdk 统一入口）接受并忽略该参数；本任务未改 M1-09 交付物以避免越界。
   **（2026-09-17 更新：已由 M1-09 增量修订关闭——Mock CLI 现接受 `--launch-token=<ULID>`
   并写入 stderr 启动日志；见 `docs/M1-09-证据.md` §1。）**

---

## 5. CI 三平台证据（M1-01 缺口修复，2026-09-18）

### 5.1 缺口与修复

- **M1-01 缺口**：原 `.github/workflows/ci.yml` 无任何平台的 `cargo test`——Linux/macOS 仅做
  `cargo check`/构建/覆盖率，Windows 仅在安全基线与 M1-09 专项中跑定向测试；M1-10 DoD3 的
  Unix `setsid`/pgrp 断言因此长期缺少 runner 证据。
- **修复**：新增 job `adapters-platform-tests`，matrix `[windows-latest, ubuntu-latest,
  macos-latest]`，三平台全量执行 `cargo test -p aether-adapters`（含 M1-09/M1-10 集成；
  矩阵内用 Bun 1.4.2 构建真实 Mock，不产生 SKIP），并追加 `--nocapture` 步骤输出平台机制序列。
- **平台断言 cfg**：Linux `#[cfg(target_os = "linux")]`（`/proc` pgrp）；macOS
  `#[cfg(target_os = "macos")]`（`ps -o pgid=`）；Windows 断言在
  `termination_sequence_platform_mechanisms_reap_whole_tree` 的 `#[cfg(windows)]` 分支。

### 5.2 绿色运行（sha `df50bc3`，run conclusion=success）

| job | URL | 结论 |
|---|---|---|
| ci run | https://github.com/Scott-PyMu/Aether/actions/runs/35323450648 | success |
| Adapters tests (windows-latest) | https://github.com/Scott-PyMu/Aether/actions/runs/35323450648/job/105530970118 | success |
| Adapters tests (ubuntu-latest) | https://github.com/Scott-PyMu/Aether/actions/runs/35323450648/job/105530970038 | success |
| Adapters tests (macos-latest) | https://github.com/Scott-PyMu/Aether/actions/runs/35323450648/job/105530969999 | success |

各平台关键输出（CI 日志摘录）：

```
# ubuntu-latest
test linux_spawn_uses_setsid_process_group ... ok
[m1-10] Unix 终止机制序列：["shutdown_rpc", "sigterm_pgid"]
test result: ok. 4 passed（m1_10_termination）；lib 109 passed；m1_09 10+11；ledger 5；supervisor 8

# macos-latest
test macos_spawn_uses_setsid_process_group ... ok
test result: ok. 4 passed（m1_10_termination）；lib 109 passed；m1_09 10+11；ledger 5；supervisor 8

# windows-latest
[m1-10] Windows 终止机制序列：["shutdown_rpc", "taskkill_tree", "terminate_job_object"]
test result: ok. 4 passed（m1_10_termination）；lib 109 passed；m1_09 10+11；ledger 5；supervisor 8
```

### 5.3 CI 暴露并修复的真实缺陷（非环境问题，未走 SKIP）

| 缺陷 | 表现 | 修复 | 提交 |
|---|---|---|---|
| Unix 整树回收 `kill` 漏杀 | GNU `kill -KILL -<pgid>` 把负 PGID 当选项解析且退出码 0，Linux 下台账回收实际未杀进程 | `kill -KILL -- -<pgid>`（GNU/BSD 通用） | `71ad5de` |
| 测试夹具进程组/僵尸判死 | 台账夹具非进程组组长；Unix 僵尸被 `kill -0` 误判存活 | `spawn_fixture` Unix 下 `process_group(0)`；`pid_alive` 僵尸显式判死（Linux `/proc`、macOS `ps`） | `71ad5de` |
| M1-09 握手竞态 | hello 后紧跟无效帧/断连时，状态先推进到 Degraded/Disconnected，握手误报 `handshake_timeout`/`protocol_error`（macOS 时序稳定触发） | 记录首次 hello；Degraded/断连/超时路径均取回已到达的 hello | `df50bc3`（见 `docs/M1-09-证据.md` 增量修订 2） |

> 说明：三平台首次运行均失败，但**失败在 `cargo test` 内部且可定位为真实实现缺陷**；
> 按任务要求「先诊断原因、不直接 skip」，以上缺陷修复后才取得三平台全绿。

---

## 6. IPC 服务契约（冻结，不接线；接线由 M2-01 承接）

### 6.1 边界与分工

- **M1-10（已完成）**：服务层语义（白名单、状态校验、`disabled → cold → starting` 转移与
  `runtime.status_changed` 广播）在 `aether-adapters::supervisor` 内实现，单测/集成覆盖见 §1；
- **M1-08（已完成）**：`runtime_retry` / `runtime_enable` 命令与 DTO 已注册，严格解析
  （`deny_unknown_fields`）+ `validate_identifier("runtime_id")` 格式校验；当前后端为
  `not_implemented`（`crates/aether-tauri/src/ipc/backend.rs:103`/`:108`）；
- **M2-01（承接方）**：实现 `IpcBackend::runtime_retry` / `runtime_enable` 薄适配——把请求
  转调本契约的服务方法并映射错误/返回值；**不得重写白名单/状态校验/转移语义**；
- 本任务不改 `aether-tauri`、不新增命令、不写 DTO；契约以代码为唯一事实源（签名原文见 6.2）。

### 6.2 服务类型与签名（自 `crates/aether-adapters/src/supervisor/runtime.rs` 提取）

```rust
pub struct Supervisor { /* 注册表：构成 runtime_id 准入白名单 */ }

impl Supervisor {
    /// 白名单查询（存在性即准入；未命中返回 None）
    pub fn get(&self, runtime_id: &str) -> Option<Arc<RuntimeSupervisor>>;      // :866

    /// 已注册 runtime_id 清单
    pub fn runtime_ids(&self) -> Vec<String>;                                   // :870

    /// runtime_retry（ADR-004）：白名单 + 仅 disabled + start_failed；
    /// 转移 disabled → cold → starting。
    pub async fn runtime_retry(&self, runtime_id: &str)
        -> Result<StartOutcome, SupervisorError>;                               // :912

    /// runtime_enable（ADR-004）：白名单 + 仅 disabled；
    /// untrusted / version_mismatch 必须先修复清单/版本（禁止直接启用）。
    pub async fn runtime_enable(&self, runtime_id: &str)
        -> Result<StartOutcome, SupervisorError>;                               // :922
}

impl RuntimeSupervisor {
    /// retry 的状态校验 + `disabled → cold` 转移（含重启策略复位）
    pub async fn begin_retry(&self) -> Result<(), SupervisorError>;             // :945
    /// enable 的状态校验 + `disabled → cold` 转移（含重启策略复位）
    pub async fn begin_enable(&self) -> Result<(), SupervisorError>;            // :958
}
```

返回值（同文件 :98）：

```rust
pub enum StartOutcome {
    Ready,                       // 完成握手 + initialize，进入 ready
    AlreadyRunning,              // 拒绝重复启动（D5「双开」预防）
    Failed {
        reason: DisabledReason,
        detail: String,
        stderr_tail: Vec<String>,
    },
    Rejected { detail: String }, // 准入拒绝：disabled + untrusted
}
```

**桥接说明（M2-01 决定，不属本契约）**：服务方法为 `async`；M1-08 的 `IpcBackend` 为同步
`Result<Value, IpcError>`。采用「async 命令直连」或「tokio 句柄桥接」由 M2-01 在命令层选择；
桥接层只允许做类型/错误映射与响应编码，不得复制、放宽或改写本节语义。

### 6.3 错误码枚举（`SupervisorError` 原文，:148）

```rust
pub enum SupervisorError {
    UnknownRuntime(String),                                        // 白名单不命中
    InvalidRuntimeId(String),                                      // 注册期：空/重复 id（非命令路径）
    RetryNotAllowed {
        status: RuntimeStatus,
        reason: Option<DisabledReason>,
    },
    EnableNotAllowed { status: RuntimeStatus },
    NeedsRemedy { reason: DisabledReason },
    Transition(String),                                            // 内部非法转移兜底
    Ledger(String),                                                // PID 台账 IO 兜底
}
```

| 触发场景 | 变体 | 当前 `Display`（原文） |
|---|---|---|
| 白名单不命中 | `UnknownRuntime(id)` | `runtime_id "<id>" 不在白名单（runtimes 注册表）` |
| 对 ready 等非 disabled 状态 retry | `RetryNotAllowed { status, reason }` | `runtime_retry 仅适用于 disabled + start_failed（当前 {status} / {reason:?}）` |
| retry 时 `disabled` 但 reason ≠ `start_failed`（如 `untrusted`/`version_mismatch`/`crash_loop`/`protocol_error`/`handshake_timeout`） | `RetryNotAllowed { status, reason }` | 同上 |
| 对非 disabled 状态 enable | `EnableNotAllowed { status }` | `runtime_enable 仅适用于 disabled（当前 {status}）` |
| `untrusted` 直接 enable | `NeedsRemedy { reason: Untrusted }` | `runtime_enable 被拒：untrusted 必须先修复清单/版本（ADR-004）` |
| `version_mismatch` 直接 enable | `NeedsRemedy { reason: VersionMismatch }` | 同上 |
| 内部状态机拒绝 / 台账 IO | `Transition` / `Ledger` | `状态转移失败：…` / `台账错误：…` |

M2-01 映射建议（不新增错误码，细目在 M2-01 验收冻结）：`UnknownRuntime` →
`IpcError` 的结构化拒绝（`invalid_enum`/`not_found` 类）；`RetryNotAllowed` /
`EnableNotAllowed` / `NeedsRemedy` → `invalid_value`（消息携带当前状态与原因）；
`Transition` / `Ledger` → `internal`。DTO 格式错误仍由 M1-08 框架在进入后端前拦截。

### 6.4 状态转移表（服务层强制；越权直接报错且**不转移**）

| 命令 | 前置条件（全部满足） | 拒绝路径（不转移） | 允许转移序列 | 失败终态 |
|---|---|---|---|---|
| `runtime_retry` | `status == disabled` 且 `status_reason == start_failed` | 其它一律 `RetryNotAllowed` | `disabled → cold`（`begin_retry`，重启策略复位）→ `cold → starting → ready` | 启动失败 → `disabled + reason`（`start_failed` / `handshake_timeout` / `version_mismatch` / `protocol_error`） |
| `runtime_enable` | `status == disabled` 且 `status_reason ∉ {untrusted, version_mismatch}` | 非 disabled → `EnableNotAllowed`；`untrusted`/`version_mismatch` → `NeedsRemedy` | 同上 | 同上 |
| `untrusted` / `version_mismatch` | —（必须**先修复清单/版本**） | 两条命令均拒绝，禁止直接启用；P3 沙箱就绪后按 D9 放宽 | — | — |

- 每次合法转移经 `SupervisorObserver::on_status_changed` 产出 `runtime.status_changed`
  （附录 B；「先日志后广播」的事件管线接线由 M2-01 完成）；
- 集成断言（`m1_10_supervisor`）：`disabled → cold → starting → ready`（enable）、
  `disabled → cold → starting`（retry），以及 `ready` 上 retry → `RetryNotAllowed`。

### 6.5 冻结声明

- 以上签名/枚举/转移表自本证据归档起**冻结**；实现以
  `crates/aether-adapters/src/supervisor/runtime.rs` 与 `protocol.rs`（`DisabledReason`）为准；
- M2-01 只做薄适配（DTO → 服务调用 → `Value`/`IpcError` 映射），不得在 IPC 层复刻或放宽校验；
- 若确需语义变更：先改 M1-10 代码与测试并更新本节，再随 M2-01 落地（两处证据互链）。

---

## 7. 资源告警集成夹具（env 阈值钩子 + 真实内存分配；M4-01 复用）

> **增量修订：资源告警集成夹具完成，M4-01 可复用。**

### 7.1 阈值环境变量（显式设置才生效；默认值严格保持 D5）

| 环境变量 | 默认 | 覆盖规则 |
|---|---|---|
| `AETHER_TEST_RSS_THRESHOLD_MB` | 1024（D5 1GB） | 显式设置且 >0 的整数 → RSS 阈值（MiB → bytes） |
| `AETHER_TEST_CPU_THRESHOLD_PCT` | 200（D5 200%） | 显式设置且为有限正数 → CPU 阈值（%） |
| `AETHER_TEST_SUSTAIN_SECS` | 60（D5 持续 60s） | 显式设置且 >0 的整数 → 持续时长（秒） |

- 未设置/非法值（空、非数字、0、NaN、负数、小数秒）一律回落 D5 默认；单测
  `env_hook_names_are_frozen` / `env_hook_overrides_thresholds` /
  `env_hook_rejects_invalid_values_and_falls_back_to_d5` 断言；
- 采样周期固定 `RESOURCE_SAMPLE_INTERVAL = 5s`，不受 env 影响；告警语义（仅告警、不自动杀、
  同一超限区间至多一次）不变；
- 生产默认路径不依赖 env：`ResourceConfig::from_env_with(|_| None) == ResourceConfig::d5()`；
- 读取点：`RuntimeSupervisor::new` → `ResourceMonitor::with_config(ResourceConfig::from_env())`。

### 7.2 集成测试（`crates/aether-adapters/tests/m1_10_resources.rs`，跨平台可跑）

夹具新增参数（`aether-adapter-fixture`，deaf/sleep/silent 模式均可用）：
`--mb <MiB>`（真实分配并逐页触写）、`--release-after-secs N`（释放回落）、
`--realloc-after-secs N`（回落后再分配）。

```
$ cargo test -p aether-adapters --test m1_10_resources -- --nocapture
[m1-10-resources] 告警#1：rss=176MiB sustained=1008ms 阈值=50MiB（limit=rss）；进程存活（D5 不自动杀）
[m1-10-resources] 持续超限 3 次采样未重复告警（告警限流）
[m1-10-resources] RSS 回落至 16MiB（< 阈值/2）→ 监视器复位
[m1-10-resources] 告警#2：rss=176MiB sustained=1067ms（复位后二次触发）；进程存活
[m1-10-resources] 链路证据：告警 → 限流 → 回落复位 → 二次告警 → 全程不杀
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 8.29s
```

断言链（真实链路：`sample_resources` → `SupervisorObserver::on_resource_alert`）：
1. **告警**：`AETHER_TEST_RSS_THRESHOLD_MB=50` + 夹具 160MiB 真实分配 → `ResourceLimitKind::Rss`
   告警（rss 176MiB > 50MiB，sustained ≈1s）；
2. **限流**：同一超限区间继续采样 3 次不重复告警（段内至多一次）；
3. **回落复位**：夹具释放内存（RSS 16MiB）→ 监视器复位且不产生新告警；
4. **二次触发**：夹具重新分配 → 复位后二次告警（证明 `reported` 复位生效）；
5. **不自动杀**：全程 `runtime.is_running()` 为真且 PID 存活（D5：仅告警）。

> 边界说明：本层的「限流」是资源告警段内去重（D5 既有语义）。「核心 OOM / 输出洪水」中
> 核心侧 delta 限流（D2/D8）的全链路验证由 **M4-01** 在该真实触发源上完成，并已在
> 《实施计划与验收标准》M4-01 DoD 追加为必过项。

### 7.3 门禁接入

`pnpm verify:m1-10` 已加入该集成测试记录（`--nocapture` 输出链路日志）；跨平台矩阵
（windows/ubuntu/macos `cargo test -p aether-adapters`）同步执行。
