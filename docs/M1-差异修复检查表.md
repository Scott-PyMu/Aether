# M1 差异修复检查表（执行版）

| 项 | 内容 |
|---|---|
| 依据基线 | 《设计文档》v1.5（冻结）、《实施计划与验收标准》v1.11、ADR-001–005 |
| 编制日期 | 2026-09-16 |
| 适用范围 | P0/Gate 1 之前的 M1 差异修复（M1-01 … M1-11） |
| 使用方式 | 逐任务认领并录入 tracker；提交信息引用任务 ID；每项 DoD 证据归档 |
| 效力说明 | 本文件为执行辅助，**不构成冻结基线的一部分**；与设计文档/实施计划冲突时，以冻结基线为准 |

---

## 0. 前置：工作区复位

- [ ] 归档提交未提交改动：`.github/workflows/ci.yml`、`Cargo.lock`、`package.json`、`pnpm-lock.yaml`、`crates/aether-adapters/{Cargo.toml,lib.rs}`
- [ ] 归档提交未跟踪文件：`crates/aether-adapters/src/{connection,framing,process,protocol}.rs`、`crates/aether-adapters/tests/`、`packages/adapter-mock/`、`packages/adapter-sdk/`、`scripts/test/m1-09/`
- [ ] 跑 `pnpm ci:local` 建立回归绿线（fmt / clippy / check / test / typecheck / build / 供应链 / m1-02/03/07/08 验证脚本）
- [ ] 复核 GitHub CI 覆盖：当前专项 job 仅见 m1-08（security-baseline）与 m1-09（wire-protocol-mock）；确认 m1-02/m1-03 的 node 验证脚本是否入 CI（未入则归 M1-01 补齐），Rust 侧契约测试由 coverage job 的 `cargo llvm-cov` 执行

---

## 1. 状态总览

| 任务 | 状态 | 一句话差距 | 优先级 |
|---|---|---|---|
| M1-01 | 已具备（复核项） | CI 骨架/供应链/覆盖率/打包/包管理器固定；仅需复核 verifier 覆盖 | — |
| M1-02 | 已具备 | 9 字段信封、严格 serde、payload 校验、契约测试全在 | — |
| M1-03 | **需修复** | 缺 `events UNIQUE(session_id, seq)`、`messages.client_msg_id` 与 0002+ 迁移、重复键审计 | **P0 前必办** |
| M1-04 | **未实现** | 无写队列/group commit/读连接池/L1-L2 接口 | 批次 2（关键路径） |
| M1-05 | **未实现** | `aether-control` 为空壳；无管线、sequencer、降级状态机 | 批次 2（关键路径） |
| M1-06 | **未实现** | 无同步盘检测/迁移流/单实例锁 | 批次 2 |
| M1-07 | 已具备 | keyring/引用/脱敏/降级及测试全在 | — |
| M1-08 | **需补齐** | 缺 ADR-004 七命令 DTO/校验；`session_send.client_msg_id` 应为必填 | 批次 2 |
| M1-09 | **需修复** | 帧上限仍 16MiB（目标 2MiB）；artifact_ref 契约未收敛 | **P0 前必办** |
| M1-10 | **未实现** | 无监督器（心跳/退避/熔断/进程组+Job Object/PID 台账/白名单） | 批次 2（Gate 1 硬条件） |
| M1-11 | **需补齐** | 缺会话恢复用例（`native_id`）与 Mode R/N 结论 | **P0 前必办** |

---

## 2. 逐任务检查表

### M1-03 SQLite 存储层与迁移 —— 需修复（P0 前必办）

- 现状：`0001_init.sql` 内嵌（`migration.rs::EMBEDDED_MIGRATIONS`）；PRAGMA、quick_check、安全模式、schema 测试（`m1_03_*`）齐备。
- 差距：
  - [ ] `events` 无 `UNIQUE(session_id, seq)`（当前仅普通索引 `idx_events_session_seq`）
  - [ ] `messages` 无 `client_msg_id` 列与 `UNIQUE(session_id, client_msg_id)`（ADR-005 决策 1）
  - [ ] 无 0002+ 迁移落地（ADR-004 决策 6 / ADR-005 决策 2）
  - [ ] 无重复 seq / `client_msg_id` 审计脚本（ADR-005 §5-4）
- 动作：
  - [ ] 新增 `migrations/0002_*.sql`：`ALTER TABLE messages ADD COLUMN client_msg_id TEXT;` + `CREATE UNIQUE INDEX idx_events_session_seq_uq ON events(session_id, seq);` + `CREATE UNIQUE INDEX idx_messages_client_msg ON messages(session_id, client_msg_id);`，并删除冗余的 `idx_events_session_seq`
  - [ ] `migration.rs` 追加 0002 到 `EMBEDDED_MIGRATIONS`
  - [ ] 新增审计脚本 `scripts/test/m1-03/audit-duplicate-keys.mjs`（只读库统计重复 `(session_id, seq)` 与 `(session_id, client_msg_id)`，输出报告）
  - [ ] **不得修改 `0001_init.sql`**
- 涉及文件：`migrations/0002_*.sql`、`crates/aether-store/src/migration.rs`、`crates/aether-store/tests/`、`scripts/test/m1-03/`
- 验证命令：`node scripts/test/m1-03/verify-m1-03.mjs`、`cargo test -p aether-store`
- DoD 证据：迁移幂等/篡改拒绝复跑；UNIQUE 重复写入被拒用例；审计报告归档

### M1-09 线协议骨架与 Mock 适配器 —— 需修复（P0 前必办）

- 现状：JSON-RPC 2.0 over JSON-Lines、连接/协议、Mock、SDK、一致性/健壮性测试、5 类工具注入 fixture、`hang` 注入均在位。
- 差距：
  - [ ] `crates/aether-adapters/src/framing.rs`：`MAX_FRAME_BYTES = 16MiB` → **2MiB**
  - [ ] `packages/adapter-sdk/src/framing.ts`：同步 2MiB
  - [ ] `artifact_ref` 引用帧 <1MiB；1–2MiB 声称 `artifact_ref` 按契约违约断连；>2MiB 任意行立即断连
  - [ ] 确认 Mock「不响应模式」脚本断言（DoD7）与「JSON-RPC 外层未知成员忽略」用例
- 动作：
  - [ ] 改常量与读取逻辑（含测试夹具的 16MiB 边界样本改为 2MiB）
  - [ ] 回归 T10（畸形帧/超长行/半行/坏 JSON）与 DoD2/DoD3 用例
- 涉及文件：`crates/aether-adapters/src/framing.rs`、`crates/aether-adapters/tests/m1_09_robustness.rs`、`packages/adapter-sdk/src/framing.ts`、`packages/adapter-mock/`
- 验证命令：`pnpm verify:m1-09`、`cargo test -p aether-adapters`
- DoD 证据：T10 报告；帧上限边界用例；吞吐基准 ≥1000 delta/s 复跑

### M1-11 A1 spike —— 需补齐（P0 前必办）

- 现状：`scripts/test/m1-11/`（codex/claude/dsh 脚本 + verify + lib）、`docs/spike/M1-11-接入笔记.md`；接入笔记已记录 Claude Code 通过、Codex 失败。
- 差距：
  - [ ] DoD2④：进程/运行时重启后用 `sessions.config.native_id` 恢复原生会话并续聊的用例
  - [ ] DoD3：接入笔记补「会话恢复能力结论（Mode R/N）」，确定 M2-02 重放语义
  - [ ] Gate 1 结论需含恢复能力（Mode R/N）
- 动作：
  - [ ] 在样例脚本中加入「重启进程 → 用 native_id 恢复 → 续聊」步骤并记录结果
  - [ ] 更新接入笔记与 verify 脚本输出
- 涉及文件：`scripts/test/m1-11/`、`docs/spike/M1-11-接入笔记.md`
- 验证命令：`node scripts/test/m1-11/verify-m1-11.mjs`（spike workflow 为 opt-in）
- DoD 证据：恢复用例脚本输出；Mode R/N 结论段落

### M1-04 单写队列与 group commit —— 未实现

- 现状：`aether-store` 为单连接直接写入（`execute_write`），未见 mpsc 写队列/批量提交/读连接池。
- 动作：
  - [ ] 新增写队列模块（`mpsc(4096)` 单写任务）
  - [ ] 16ms 或 ≥256 条 group commit；单事务目标 <500ms
  - [ ] 4 读连接池
  - [ ] `>1024` L1 告警事件；`>4096` 暴露 `storage_backpressure` 接口（供 M2-04）
- 涉及文件：`crates/aether-store/src/`、`crates/aether-store/tests/`
- 验证命令：`cargo test -p aether-store`；新增基准（1k 事件写事务 P95<50ms、读 P95<10ms）
- DoD 证据：基准输出；批量参数断言；队列深度诊断

### M1-05 事件管线 —— 未实现（依赖 M1-04、M1-03 DoD5）

- 现状：`aether-control` 空壳（lib.rs 注明预留 M1-05/M2-01）；`aether-core` 仅有信封与类型。
- 动作：
  - [ ] normalizer → 严格校验 → 会话 sequencer（seq 单调）→ journal 入队 → 写成功后才广播
  - [ ] delta 合并（16ms/8KB）；`message.completed` 终稿
  - [ ] `last_seq` 补读；>10k 拒绝自动补发并返回错误码
  - [ ] 写失败重试 3 次 → `persist_degraded` + 只读；拒绝新写入/新 run；未落盘不广播；在途 run 转 `cancelled`；降级通知经 `health`
  - [ ] DB `UNIQUE(session_id, seq)` 兜底：重复 seq 视为管线 bug 并计入诊断
- 涉及文件：`crates/aether-control/src/`、`crates/aether-store/`、`crates/aether-tauri/`（health 透出）
- 验证命令：`cargo test -p aether-control`、`cargo test --workspace`；新增 verifier
- DoD 证据：乱序/重复注入 10k 属性测试；故障注入调用序断言；sequencer 崩溃恢复

### M1-06 数据目录检测、迁移流与单实例锁 —— 未实现

- 现状：检索不到 OneDrive/CloudStorage/单实例相关实现。
- 动作：
  - [ ] Windows 三类：OneDrive 环境变量前缀祖先、父目录重解析点、注册表 `UserFolder`
  - [ ] macOS 两类：iCloud ubiquitous、`~/Library/CloudStorage/*`
  - [ ] 命中后仅「迁移/退出」（主界面不可达）；迁移执行（复制→校验→原子替换）
  - [ ] 单实例锁（二次启动聚焦已有窗口）
- 涉及文件：`crates/aether-tauri/src/`（启动序列）、`crates/aether-store/`、`scripts/test/`
- 验证命令：新增 verifier；T12/T13
- DoD 证据：Win/mac 样本识别报告；本地目录对照 20 例放行；迁移 E2E

### M1-08 Tauri 壳与安全基线 —— 需补齐

- 现状：CSP、导航拦截、IPC 参数校验框架、capabilities、E2E 在位于位。
- 差距：
  - [ ] ADR-004 七命令 DTO 与校验：`backup_list`、`backup_restore`、`app_restart`、`run_retry`、`runtime_retry`、`runtime_enable`、`workspace_set`（含 `run_id` ULID/终态、白名单、`confirm:true`、路径 canonicalize 与同步盘拒绝）
  - [ ] `session_send.client_msg_id` 由 optional 改为**必填 ULID**（ADR-005 幂等键）
- 涉及文件：`crates/aether-tauri/src/ipc/{dto,validate,commands,backend}.rs`、`scripts/test/m1-08/`
- 验证命令：`pnpm verify:m1-08`
- DoD 证据：校验矩阵新增命令用例；畸形参数全部结构化错误且不落库

### M1-10 适配器监督器 —— 未实现（Gate 1 硬条件）

- 现状：`crates/aether-adapters/src/process.rs`（167 行）仅 spawn/stdout-stdin/stderr 尾行/等待/终止帮助器。
- 动作：
  - [ ] 状态机 `cold → starting → ready → degraded → disabled`（含 `degraded → ready`、`disabled → cold/starting` 恢复转移）与 `status_reason`（含 `storage_backpressure`）
  - [ ] 预热；心跳 10s×连续 3 次失败；退避 1/2/4/8/16/30s；60s ≥5 次崩溃熔断 `crash_loop`
  - [ ] 进程组（setsid / CREATE_NEW_PROCESS_GROUP）+ Windows Job Object（`TerminateJobObject` 优先，`taskkill /T /F` 兜底）
  - [ ] PID 台账三条件（存活 + 启动时间一致 + `launch_token` 命中）；准入白名单；资源采样
  - [ ] IPC：`runtime_retry`（`disabled + start_failed`）、`runtime_enable`（`disabled`；`untrusted`/`version_mismatch` 需先修复）
- 涉及文件：`crates/aether-adapters/src/`（新增 supervisor）、`crates/aether-tauri/src/ipc/`、`scripts/test/`
- 验证命令：`cargo test -p aether-adapters`；新增 verifier（平台断言）
- DoD 证据：退避/熔断单测；Job Object 终止断言；PID 复用夹具；T5a/T5b 脚本

### M1-01 / M1-02 / M1-07（已具备，保持）

- [ ] M1-01：`pnpm ci:local` 绿线；复核 GitHub CI 的 m1-02/m1-03 verifier 覆盖（见 §0）
- [ ] M1-02：`pnpm verify:m1-02` 复跑
- [ ] M1-07：`pnpm verify:m1-07` 复跑

---

## 3. 建议批次与出口

### 批次 1（P0 前必办，可并行）
M1-03 DoD5 ∥ M1-09 DoD8 ∥ M1-11 DoD2④/DoD3
- 出口：`pnpm ci:local` 全绿；m1-03 verifier 覆盖 UNIQUE/client_msg_id 断言；T10 通过；spike 恢复结论归档

### 批次 2（Gate 1 主体）
M1-04 → M1-05；M1-10；M1-06；M1-08 补齐
- 出口：clippy 0 告警；`cargo test --workspace`；`pnpm verify:m1-08`、`pnpm verify:m1-09`；新增 m1-04/05/06/10 verifier

### 批次 3（Gate 1 收口）
M1-01…M1-11 证据汇总 + Gate 1 评审（含恢复能力 Mode R/N）
- 出口：Gate 1 条件逐条出示证据；tracker 更新

---

## 4. 硬约束提醒（摘自 AGENTS.md 与冻结基线）

- 不修改 `0001_init.sql`；差异一律 0002+，迁移前出具重复键审计（ADR-004 决策 6 / ADR-005 决策 2）
- 核心 crate 禁 `unwrap/expect/panic`；命令层全 `Result`
- 先日志后广播；`events UNIQUE(session_id, seq)` 为 DB 兜底
- 帧上限 2MiB、`artifact_ref` <1MiB；超限断连
- 权限门仅约束经线协议上报的工具调用；适配器进程内行为不经此门
- 不改生成物（bindings 尚未生成，T14 属 M3-01）；无新增未批准依赖
- 提交信息引用任务 ID；单任务范围内改动

## 5. 跟踪与计数

- 任务计数：文档条目 37 / 互斥执行 36（M2-11 条件成立时 35）
- P0 前必办（tracker 单独标记）：M1-03 DoD5、M1-09 DoD8、M1-11 DoD2④/DoD3
- 随任务承接：ADR-005 §5 四项、ADR-004 §5 第 5 条（M1-03 DoD5 / M1-09 DoD8）
