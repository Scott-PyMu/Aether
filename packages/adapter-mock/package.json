{
  "name": "@aether/adapter-mock",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Aether Mock 适配器（Bun 单文件）：工具调用注入清单与吞吐基准（M1-09 DoD5/DoD6）",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "build:bun": "bun build src/main.ts --compile --outfile dist/aether-mock-adapter"
  },
  "dependencies": {
    "@aether/adapter-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.20.2",
    "@vitest/coverage-v8": "2.1.9",
    "typescript": "5.9.3",
    "vitest": "2.1.9"
  }
}
